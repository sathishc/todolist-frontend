version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo $AWS_API_NAME
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
